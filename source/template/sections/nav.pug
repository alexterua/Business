nav.nav
  -var items = ['Главная', 'О нас', 'Наши преимущества', 'Наши работы']
  each item in items
    if item === 'Главная'
      a.nav__link.nav__link--active=item
    else
      a.nav__link(href="#")=item